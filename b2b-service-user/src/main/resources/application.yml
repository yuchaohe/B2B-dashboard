server:
  port: 9001

spring:
  application:
    name: b2b-service-user
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
